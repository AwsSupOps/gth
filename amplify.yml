version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build/webapp/res
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
